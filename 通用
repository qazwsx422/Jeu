-- 基础可拖动GUI (防封版)
local DragGUI = {}

-- 防封措施
local AntiBan = {
    -- 随机化GUI名称
    RandomNames = {"UI_System", "GamePanel", "ControlCenter", "SettingsMenu", "PlayerHUD"},
    -- 随机化颜色
    RandomColors = {
        Color3.fromRGB(35, 35, 40),
        Color3.fromRGB(45, 45, 50), 
        Color3.fromRGB(40, 40, 45),
        Color3.fromRGB(50, 50, 55)
    },
    -- 延迟执行
    DelayExecute = function(callback, minDelay, maxDelay)
        minDelay = minDelay or 0.1
        maxDelay = maxDelay or 0.5
        local delay = math.random(minDelay * 1000, maxDelay * 1000) / 1000
        wait(delay)
        callback()
    end
}

-- 安全执行函数
function SafeExecute(func, errorMsg)
    local success, result = pcall(func)
    if not success then
        warn("安全执行错误: " .. tostring(result))
        return nil
    end
    return result
end

-- 汉化版墨水游戏脚本
local 墨水游戏 = {}

-- 游戏配置
墨水游戏.配置 = {
    游戏名称 = "墨水大作战",
    版本 = "2.0",
    作者 = "汉化版",
    背景颜色 = Color3.fromRGB(20, 20, 30),
    主色调 = Color3.fromRGB(0, 150, 255),
    副色调 = Color3.fromRGB(255, 100, 100)
}

-- 玩家数据
墨水游戏.玩家数据 = {}
墨水游戏.游戏界面 = nil
墨水游戏.游戏进行中 = false

-- 初始化游戏
function 墨水游戏:初始化()
    print("🎮 正在初始化 " .. self.配置.游戏名称 .. " ...")
    
    -- 清理旧界面
    if self.游戏界面 then
        self.游戏界面:Destroy()
    end
    
    -- 创建游戏界面
    self:创建界面()
    
    -- 设置游戏控制
    self:设置控制()
    
    print("✅ " .. self.配置.游戏名称 .. " 初始化完成!")
    print("💡 点击'开始游戏'按钮开始游玩")
end

-- 创建游戏界面
function 墨水游戏:创建界面()
    -- 创建主界面
    local 游戏界面 = Instance.new("ScreenGui")
    游戏界面.Name = "墨水游戏界面"
    游戏界面.ResetOnSpawn = false
    游戏界面.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
    
    -- 主框架
    local 主框架 = Instance.new("Frame")
    主框架.Size = UDim2.new(0, 350, 0, 400)
    主框架.Position = UDim2.new(0.5, -175, 0.5, -200)
    主框架.BackgroundColor3 = self.配置.背景颜色
    主框架.BorderSizePixel = 0
    主框架.Parent = 游戏界面
    
    -- 圆角
    local 圆角 = Instance.new("UICorner")
    圆角.CornerRadius = UDim.new(0, 15)
    圆角.Parent = 主框架
    
    -- 边框
    local 边框 = Instance.new("UIStroke")
    边框.Color = Color3.fromRGB(100, 100, 150)
    边框.Thickness = 2
    边框.Parent = 主框架
    
    -- 标题
    local 标题 = Instance.new("TextLabel")
    标题.Size = UDim2.new(1, 0, 0, 60)
    标题.BackgroundTransparency = 1
    标题.Text = "🎨 墨水大作战"
    标题.TextColor3 = Color3.fromRGB(255, 255, 255)
    标题.TextSize = 24
    标题.Font = Enum.Font.GothamBold
    标题.Parent = 主框架
    
    -- 状态显示
    local 状态框 = Instance.new("Frame")
    状态框.Size = UDim2.new(0.8, 0, 0, 80)
    状态框.Position = UDim2.new(0.1, 0, 0.2, 0)
    状态框.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    状态框.BorderSizePixel = 0
    状态框.Parent = 主框架
    
    local 状态圆角 = Instance.new("UICorner")
    状态圆角.CornerRadius = UDim.new(0, 10)
    状态圆角.Parent = 状态框
    
    -- 墨水值显示
    local 墨水文本 = Instance.new("TextLabel")
    墨水文本.Size = UDim2.new(1, 0, 0.5, 0)
    墨水文本.Position = UDim2.new(0, 0, 0, 0)
    墨水文本.BackgroundTransparency = 1
    墨水文本.Text = "💧 墨水值: 100/100"
    墨水文本.TextColor3 = Color3.fromRGB(255, 255, 255)
    墨水文本.TextSize = 16
    墨水文本.Font = Enum.Font.GothamSemibold
    墨水文本.Parent = 状态框
    
    -- 分数显示
    local 分数文本 = Instance.new("TextLabel")
    分数文本.Size = UDim2.new(1, 0, 0.5, 0)
    分数文本.Position = UDim2.new(0, 0, 0.5, 0)
    分数文本.BackgroundTransparency = 1
    分数文本.Text = "🏆 分数: 0"
    分数文本.TextColor3 = Color3.fromRGB(255, 255, 255)
    分数文本.TextSize = 16
    分数文本.Font = Enum.Font.GothamSemibold
    分数文本.Parent = 状态框
    
    -- 开始游戏按钮
    local 开始按钮 = Instance.new("TextButton")
    开始按钮.Size = UDim2.new(0, 200, 0, 45)
    开始按钮.Position = UDim2.new(0.5, -100, 0.5, -22)
    开始按钮.BackgroundColor3 = self.配置.主色调
    开始按钮.Text = "开始游戏"
    开始按钮.TextColor3 = Color3.fromRGB(255, 255, 255)
    开始按钮.TextSize = 18
    开始按钮.Font = Enum.Font.GothamBold
    开始按钮.Parent = 主框架
    
    local 开始按钮圆角 = Instance.new("UICorner")
    开始按钮圆角.CornerRadius = UDim.new(0, 10)
    开始按钮圆角.Parent = 开始按钮
    
    -- 攻击按钮
    local 攻击按钮 = Instance.new("TextButton")
    攻击按钮.Size = UDim2.new(0, 150, 0, 40)
    攻击按钮.Position = UDim2.new(0.5, -75, 0.7, -20)
    攻击按钮.BackgroundColor3 = self.配置.副色调
    攻击按钮.Text = "🎯 攻击"
    攻击按钮.TextColor3 = Color3.fromRGB(255, 255, 255)
    攻击按钮.TextSize = 16
    攻击按钮.Font = Enum.Font.GothamSemibold
    攻击按钮.Parent = 主框架
    
    local 攻击按钮圆角 = Instance.new("UICorner")
    攻击按钮圆角.CornerRadius = UDim.new(0, 8)
    攻击按钮圆角.Parent = 攻击按钮
    
    -- 关闭按钮
    local 关闭按钮 = Instance.new("TextButton")
    关闭按钮.Size = UDim2.new(0, 120, 0, 35)
    关闭按钮.Position = UDim2.new(0.5, -60, 0.85, -17)
    关闭按钮.BackgroundColor3 = Color3.fromRGB(80, 80, 90)
    关闭按钮.Text = "关闭游戏"
    关闭按钮.TextColor3 = Color3.fromRGB(255, 255, 255)
    关闭按钮.TextSize = 14
    关闭按钮.Font = Enum.Font.Gotham
    关闭按钮.Parent = 主框架
    
    local 关闭按钮圆角 = Instance.new("UICorner")
    关闭按钮圆角.CornerRadius = UDim.new(0, 6)
    关闭按钮圆角.Parent = 关闭按钮
    
    -- 存储UI引用
    self.界面元素 = {
        主界面 = 游戏界面,
        墨水文本 = 墨水文本,
        分数文本 = 分数文本,
        开始按钮 = 开始按钮,
        攻击按钮 = 攻击按钮,
        关闭按钮 = 关闭按钮
    }
    
    self.游戏界面 = 游戏界面
end

-- 设置游戏控制
function 墨水游戏:设置控制()
    local 元素 = self.界面元素
    
    -- 开始游戏按钮事件
    元素.开始按钮.MouseButton1Click:Connect(function()
        self:开始游戏()
    end)
    
    -- 攻击按钮事件
    元素.攻击按钮.MouseButton1Click:Connect(function()
        self:攻击()
    end)
    
    -- 关闭按钮事件
    元素.关闭按钮.MouseButton1Click:Connect(function()
        self:停止游戏()
    end)
end

-- 开始游戏
function 墨水游戏:开始游戏()
    if self.游戏进行中 then
        return
    end
    
    print("🚀 开始游戏!")
    self.游戏进行中 = true
    
    -- 初始化玩家数据
    local 玩家 = game.Players.LocalPlayer
    self.玩家数据[玩家] = {
        墨水值 = 100,
        分数 = 0,
        是否在游戏中 = true
    }
    
    -- 更新按钮状态
    self.界面元素.开始按钮.Text = "游戏中..."
    self.界面元素.开始按钮.BackgroundColor3 = Color3.fromRGB(0, 200, 80)
    
    -- 开始游戏循环
    self:游戏循环()
end

-- 游戏主循环
function 墨水游戏:游戏循环()
    spawn(function()
        while self.游戏进行中 and self.玩家数据[game.Players.LocalPlayer] do
            wait(0.1)
            
            -- 恢复墨水
            self:恢复墨水()
            
            -- 更新界面
            self:更新界面()
        end
    end)
end

-- 恢复墨水
function 墨水游戏:恢复墨水()
    local 玩家数据 = self.玩家数据[game.Players.LocalPlayer]
    if 玩家数据 then
        玩家数据.墨水值 = math.min(玩家数据.墨水值 + 0.5, 100)
    end
end

-- 更新界面
function 墨水游戏:更新界面()
    local 玩家数据 = self.玩家数据[game.Players.LocalPlayer]
    if 玩家数据 and self.界面元素 then
        self.界面元素.墨水文本.Text = "💧 墨水值: " .. math.floor(玩家数据.墨水值) .. "/100"
        self.界面元素.分数文本.Text = "🏆 分数: " .. 玩家数据.分数
    end
end

-- 攻击功能
function 墨水游戏:攻击()
    if not self.游戏进行中 then
        print("❌ 请先开始游戏!")
        return
    end
    
    local 玩家数据 = self.玩家数据[game.Players.LocalPlayer]
    if 玩家数据 and 玩家数据.墨水值 >= 15 then
        玩家数据.墨水值 = 玩家数据.墨水值 - 15
        玩家数据.分数 = 玩家数据.分数 + 1
        print("🎯 攻击成功! 分数: " .. 玩家数据.分数)
        return true
    else
        print("💧 墨水不足!")
        return false
    end
end

-- 停止游戏
function 墨水游戏:停止游戏()
    self.游戏进行中 = false
    
    if self.游戏界面 then
        self.游戏界面:Destroy()
        self.游戏界面 = nil
    end
    
    print("🛑 游戏结束")
end

-- 导出墨水游戏
getgenv().墨水游戏 = 墨水游戏

function DragGUI:Create(title)
    -- 防封措施：随机名称和颜色
    local randomName = AntiBan.RandomNames[math.random(1, #AntiBan.RandomNames)]
    local randomColor = AntiBan.RandomColors[math.random(1, #AntiBan.RandomColors)]
    
    -- 安全创建实例
    local function SafeInstance(className, properties)
        return SafeExecute(function()
            local instance = Instance.new(className)
            for prop, value in pairs(properties or {}) do
                instance[prop] = value
            end
            return instance
        end, "创建实例失败: " .. className)
    end

    -- 创建主界面
    local ScreenGui = SafeInstance("ScreenGui", {
        Parent = game:GetService("Players").LocalPlayer:WaitForChild("PlayerGui"),
        Name = randomName,
        ResetOnSpawn = false,
        ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    })
    
    if not ScreenGui then return nil end

    -- 主框架
    local MainFrame = SafeInstance("Frame", {
        Parent = ScreenGui,
        Size = UDim2.new(0, 300, 0, 400),
        Position = UDim2.new(0.5, -150, 0.5, -200),
        BackgroundColor3 = randomColor,
        BorderSizePixel = 0,
        ClipsDescendants = true
    })
    
    -- 圆角
    local corner = SafeInstance("UICorner", {
        Parent = MainFrame,
        CornerRadius = UDim.new(0, 8)
    })
    
    -- 顶部栏（可拖动区域）
    local TopBar = SafeInstance("Frame", {
        Parent = MainFrame,
        Size = UDim2.new(1, 0, 0, 30),
        BackgroundColor3 = Color3.fromRGB(25, 25, 30),
        BorderSizePixel = 0
    })
    
    local topCorner = SafeInstance("UICorner", {
        Parent = TopBar,
        CornerRadius = UDim.new(0, 8)
    })

    -- 标题
    local Title = SafeInstance("TextLabel", {
        Parent = TopBar,
        Size = UDim2.new(1, -60, 1, 0),
        Position = UDim2.new(0, 10, 0, 0),
        BackgroundTransparency = 1,
        Text = title or "系统面板",
        TextColor3 = Color3.fromRGB(255, 255, 255),
        TextSize = 14,
        TextXAlignment = Enum.TextXAlignment.Left,
        Font = Enum.Font.Gotham
    })

    -- 关闭按钮
    local CloseBtn = SafeInstance("TextButton", {
        Parent = TopBar,
        Size = UDim2.new(0, 25, 0, 25),
        Position = UDim2.new(1, -30, 0, 2),
        BackgroundColor3 = Color3.fromRGB(80, 80, 90),
        BorderSizePixel = 0,
        Text = "×",
        TextColor3 = Color3.fromRGB(255, 255, 255),
        TextSize = 16,
        Font = Enum.Font.GothamBold
    })
    
    local closeCorner = SafeInstance("UICorner", {
        Parent = CloseBtn,
        CornerRadius = UDim.new(0, 4)
    })

    -- 内容区域
    local Content = SafeInstance("Frame", {
        Parent = MainFrame,
        Size = UDim2.new(1, 0, 1, -30),
        Position = UDim2.new(0, 0, 0, 30),
        BackgroundColor3 = Color3.fromRGB(45, 45, 50),
        BorderSizePixel = 0
    })
    
    local contentCorner = SafeInstance("UICorner", {
        Parent = Content,
        CornerRadius = UDim.new(0, 8)
    })

    -- 拖动功能
    local dragging = false
    local dragInput, dragStart, startPos
    
    local function update(input)
        SafeExecute(function()
            local delta = input.Position - dragStart
            MainFrame.Position = UDim2.new(
                startPos.X.Scale, 
                startPos.X.Offset + delta.X, 
                startPos.Y.Scale, 
                startPos.Y.Offset + delta.Y
            )
        end, "拖动更新失败")
    end

    -- 绑定拖动事件
    AntiBan.DelayExecute(function()
        SafeExecute(function()
            TopBar.InputBegan:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 then
                    dragging = true
                    dragStart = input.Position
                    startPos = MainFrame.Position
                    
                    input.Changed:Connect(function()
                        if input.UserInputState == Enum.UserInputState.End then
                            dragging = false
                        end
                    end)
                end
            end)
            
            TopBar.InputChanged:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseMovement and dragging then
                    dragInput = input
                end
            end)
            
            game:GetService("UserInputService").InputChanged:Connect(function(input)
                if input == dragInput and dragging then
                    update(input)
                end
            end)
        end, "拖动事件绑定失败")
    end)

    -- 关闭功能
    CloseBtn.MouseButton1Click:Connect(function()
        AntiBan.DelayExecute(function()
            SafeExecute(function()
                ScreenGui:Destroy()
            end, "关闭GUI失败")
        end, 0, 0.1)
    end)

    return Content
end

-- 安全加载脚本函数
function SafeLoadScript(url)
    AntiBan.DelayExecute(function()
        SafeExecute(function()
            if not string.find(url, "^https://") then
                warn("不安全的URL: " .. url)
                return
            end
            
            print("🔒 安全加载中...")
            local content = game:HttpGet(url, true)
            
            if #content < 10 then
                warn("脚本内容过短，可能无效")
                return
            end
            
            local fn, err = loadstring(content)
            if fn then
                local success, result = pcall(fn)
                if success then
                    print("✅ 脚本加载成功!")
                else
                    warn("❌ 脚本执行错误: " .. tostring(result))
                end
            else
                warn("❌ 脚本编译错误: " .. tostring(err))
            end
        end, "加载脚本失败")
    end, 0.2, 0.5)
end

-- 使用示例
AntiBan.DelayExecute(function()
    local content = DragGUI:Create("脚本加载器")
    
    if content then
        -- 本地墨水游戏按钮
        local localInkGameBtn = SafeInstance("TextButton", {
            Parent = content,
            Size = UDim2.new(0, 200, 0, 40),
            Position = UDim2.new(0.5, -100, 0.2, 0),
            BackgroundColor3 = Color3.fromRGB(120, 80, 160),
            BorderSizePixel = 0,
            Text = "🎨 本地墨水游戏",
            TextColor3 = Color3.fromRGB(255, 255, 255),
            TextSize = 14,
            Font = Enum.Font.Gotham
        })
        
        if localInkGameBtn then
            local btnCorner = SafeInstance("UICorner", {
                Parent = localInkGameBtn,
                CornerRadius = UDim.new(0, 6)
            })
            
            localInkGameBtn.MouseButton1Click:Connect(function()
                print("🎮 启动本地墨水游戏...")
                墨水游戏:初始化()
            end)
        end

        -- 在线墨水游戏按钮
        local onlineInkGameBtn = SafeInstance("TextButton", {
            Parent = content,
            Size = UDim2.new(0, 200, 0, 40),
            Position = UDim2.new(0.5, -100, 0.4, 0),
            BackgroundColor3 = Color3.fromRGB(80, 160, 120),
            BorderSizePixel = 0,
            Text = "🌐 在线墨水游戏",
            TextColor3 = Color3.fromRGB(255, 255, 255),
            TextSize = 14,
            Font = Enum.Font.Gotham
        })
        
        if onlineInkGameBtn then
            local btnCorner = SafeInstance("UICorner", {
                Parent = onlineInkGameBtn,
                CornerRadius = UDim.new(0, 6)
            })
            
            onlineInkGameBtn.MouseButton1Click:Connect(function()
                SafeLoadScript("https://raw.githubusercontent.com/VapeVoidware/VW-Add/main/inkgame.lua")
            end)
        end

        -- Pastebin脚本按钮
        local pastebinBtn = SafeInstance("TextButton", {
            Parent = content,
            Size = UDim2.new(0, 200, 0, 40),
            Position = UDim2.new(0.5, -100, 0.6, 0),
            BackgroundColor3 = Color3.fromRGB(0, 100, 200),
            BorderSizePixel = 0,
            Text = "📋 加载Pastebin脚本",
            TextColor3 = Color3.fromRGB(255, 255, 255),
            TextSize = 14,
            Font = Enum.Font.Gotham
        })
        
        if pastebinBtn then
            local btnCorner = SafeInstance("UICorner", {
                Parent = pastebinBtn,
                CornerRadius = UDim.new(0, 6)
            })
            
            pastebinBtn.MouseButton1Click:Connect(function()
                SafeLoadScript("https://pastebin.com/raw/Up3P2KBp")
            end)
        end
        
        print("🎮 GUI已加载完成")
        print("💡 现在包含本地汉化版墨水游戏!")
    end
end)
